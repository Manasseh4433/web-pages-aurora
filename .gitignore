The file paths in your input appear to be corrupted or improperly encoded Russian text that translates roughly to "Texts" when decoded. Since these are not recognizable build artifacts, dependency directories, temporary files, or other items that should be ignored, and they appear to be source/config files, no entries need to be added to .gitignore.

(empty)